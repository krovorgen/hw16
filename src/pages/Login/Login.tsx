import React from 'react';
import cn from 'classnames';

import styles from './Login.module.scss';

export const Login = () => {
  return <div className={cn('container', styles.root)}>Login</div>;
};
